## Scenario 2 — Employee User Provisioning Automation

### Task 2: Automate Employee User Provisioning

#### Scenario

The Human Resources department has onboarded several new employees and requires local Linux user accounts to be provisioned automatically using a reusable administrative script.

#### Objectives

* Create a Bash script named **create-users.sh**.
* Create the following local user accounts:
  * employee1
  * employee2
  * employee3
* Assign the password:

```text
P@ssw0rd123!
```

* Configure each account so that passwords never expire.
* Handle scenarios where users already exist.
* Display appropriate status messages for each operation.
* Generate a report named **/home/labuser/reports/UserCreationReport.txt**.

#### Report Requirements

The report must contain:

* Username
* Creation Status
* Date and Time

<validation step="Employee User Provisioning" />

### You have successfully completed the Assessment.